# Steps:
#   1. Import generate_partition from partition_utils.py.
#   2. Define simulation parameters (num_objects, alpha).
#   3. Generate a partition using generate_partition.
#   4. Print out the partition (each group on a separate line).